[phases.setup]
nixPkgs = ['...', 'chromium', 'glib', 'nss', 'atk']

[phases.build]
cmds = ['npm install']

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "false"
PUPPETEER_EXECUTABLE_PATH = "/usr/bin/chromium"